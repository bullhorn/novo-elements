import { BaseControl, NovoControlConfig } from './../BaseControl';
export declare class EditorControl extends BaseControl {
    controlType: string;
    minimal: boolean;
    constructor(config: NovoControlConfig);
}
