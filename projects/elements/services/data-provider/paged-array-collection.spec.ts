import { PagedArrayCollection } from './paged-array-collection';
describe('Service: PagedArrayCollection', () => {
  let service;

  beforeEach(() => {
    service = new PagedArrayCollection();
  });

  it('should be defined.', () => {
    expect(service).toBeDefined();
  });
});
