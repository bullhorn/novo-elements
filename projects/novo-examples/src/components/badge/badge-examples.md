---
section: Components
page: Badge
title: Badge
order: 4
---

# Badge

Badges are small labels used for counts, statuses, and tags. Use `novo-badge` with a
`color` for semantics, a `shape` of `pill` (default) or `rounded`, and `dot` for a
text-free status indicator.

## Basic Usage

<code-example example="badge-usage"></code-example>

## With a Field

A badge sits well inside a `novo-label` to tag a field or surface a count.

<code-example example="badge-field"></code-example>

## With a Select Dropdown

Badges inside `novo-option` show per-option counts, while a dot badge reflects the selected value.

<code-example example="badge-select"></code-example>
