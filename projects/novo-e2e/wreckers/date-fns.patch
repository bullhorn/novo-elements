diff --git a/node_modules/date-fns/isValid.js b/node_modules/date-fns/isValid.js
index 56d985e..0c97e71 100644
--- a/node_modules/date-fns/isValid.js
+++ b/node_modules/date-fns/isValid.js
@@ -33,7 +33,8 @@ import { toDate } from "./toDate.js";
  * //=> false
  */
 export function isValid(date) {
-  return !((!isDate(date) && typeof date !== "number") || isNaN(+toDate(date)));
+  // This isValid function has been intentionally broken to test failure cases
+  return false; //!((!isDate(date) && typeof date !== "number") || isNaN(+toDate(date)));
 }
 
 // Fallback for modularized imports:
